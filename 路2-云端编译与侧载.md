# 路 2：云端编译 + AltStore 侧载

目标：GitHub 的云端 Mac 帮你编译出 `AgentBook.ipa`，然后用 AltStore 装到 iPhone 上。
**你的 Intel Mac 全程不需要装 Xcode。**

整体流程：

```
你的 Mac              GitHub 云端           iPhone
────────             ──────────           ──────────
推送代码   ──────▶   编译出 .ipa
                     （3-6 分钟）
下载 .ipa  ◀──────
AirDrop 给手机 ─────────────────────▶     Files 收到
                                          AltStore 安装（用你的 Apple ID 签名）
```

预计耗时：第一次约 30 分钟（大部分花在装 AltStore 和 Apple ID 流程上）。

---

## 第 0 步：先确认三件事

| 检查项 | 要求 | 怎么看 |
| --- | --- | --- |
| iPhone 系统 | iOS 14 以上（推荐 16+） | 设置 → 通用 → 关于本机 |
| Apple ID 双重认证 | **必须开启** | 设置 → 顶部头像 → 登录与安全 |
| 电脑 | 能装 AltServer（Mac 或 Windows 都行） | Intel Mac 完全没问题 |

**额外好康**：如果你的 iPhone 是 **iOS 17.0 或更低**（14.0–16.6.1、17.0），
那可以用 **TrollStore**，装完**永久有效、不过期、不需要电脑**。
把 IPA 传到手机上，用 TrollStore 打开即可。先去 TrollStore 官网确认系统版本在支持范围内。

---

## 第 1 步：把工程推到 GitHub

### 1.1 先创建一个空仓库

1. 登录 github.com，右上角 **+** → **New repository**
2. 填仓库名，比如 `agent-book-ios`
3. **Public 还是 Private？**

| | Public | Private |
| --- | --- | --- |
| Actions 额度 | 免费无限 | 每月 2000 分钟（macOS 按 10 倍算，约 40 次编译，够用） |
| 内容公开 | 任何人可见 | 只有你自己 |

想保密就选 **Private**，额度够你这一个工程用很久。

4. **不要**勾选 Add a README / .gitignore / license（勾了会让第一次推送冲突）
5. 点 **Create repository**，把它给出的地址复制下来

### 1.2 推送

双击这个文件，按提示走：

**`推送到 GitHub.command`**

它会：初始化 git → 检查身份 → 列出要提交的文件让你确认 → 让你粘贴仓库地址 → 推送。
（双击打不开就右键 → 打开；或先在终端执行 `chmod +x "推送到 GitHub.command"`）

**关于凭据**：GitHub 现在不接受账号密码，需要 Personal Access Token。

1. GitHub 右上角头像 → Settings → 最下方 Developer settings
2. Personal access tokens → **Tokens (classic)** → Generate new token (classic)
3. 勾选 **repo**，有效期选 90 天
4. 生成后**立刻复制**（只显示一次）；推送时用户名填 GitHub 用户名，密码位置粘这个 token

Mac 会把它存进钥匙串，以后不用再输。

### 不想用终端？

也可以网页上传：打开仓库 → **Add file → Upload files** → 把 `AgentBook` 文件夹拖进去。
**但 `.github` 是隐藏目录，网页上传很容易漏掉它，一漏掉云端就不会自动编译。**
所以还是建议用上面那个脚本。

---

## 第 2 步：等云端编译

推送完成后：

1. 打开仓库页面 → 上方 **Actions** 标签
2. 会看到「云端编译 IPA」在跑（黄点），**第一次约 3-6 分钟**
3. 变成绿勾就成功了
4. 点进这次运行，页面**最下方 Artifacts** 里下载 `AgentBook-ipa.zip`
5. 解压得到 **`AgentBook.ipa`**

### 如果变红了（失败）

点进去展开红色步骤，把报错日志发我，我直接改代码。最可能的两种失败：

- `xcodebuild -list` 报找不到 scheme —— 我在工程里加过共享 scheme，理论上不会
- 编译某个 Swift 文件报错 —— 我本地没有 Xcode 无法编译验证，这正是需要你把报错带回来的地方

---

## 第 3 步：装 AltStore（做一次）

### ⚠️ iOS 16 以上必做：打开「开发者模式」

从 iOS 16 开始，苹果要求**必须手动打开开发者模式**，否则自签的 App 装上也打不开
（报错通常是「无法验证 App」或点图标直接闪退）。**iOS 26 同样需要。**

打开方式：

1. 设置 → **隐私与安全性** → 拉到最下面 → **开发者模式**
2. 打开开关 → 会要求**重启手机**
3. 重启后会出现确认弹窗 → 点「打开」

注意：**「开发者模式」这一项在你第一次用 AltServer 成功连接手机之前是不显示的。**
所以顺序是：装完 AltStore → 回设置里找这一项 → 打开 → 重启。

---

1. 电脑上从 altstore.io 下载 **AltServer**（Mac / Windows 版都有）
2. 运行 AltServer（出现在菜单栏 / 托盘）
3. iPhone 用数据线连电脑（**只有这一步需要插线**）
4. 点菜单栏 AltServer 图标 → **Install AltStore** → 选你的 iPhone
5. 输入 Apple ID 和密码（苹果官方签名流程，AltServer 只是代你操作）
6. 手机上出现 **AltStore** 图标，第一次点开会提示不受信任：
   设置 → 通用 → VPN与设备管理 → 开发者 App → 信任你的 Apple ID
7. 拔掉数据线

之后 AltStore 会通过 Wi-Fi 自动续签，只要电脑上的 AltServer 在运行。

---

## 第 4 步：把 IPA 装进手机

1. Mac 上右键 `AgentBook.ipa` → **共享 → 隔空投送** → 选你的 iPhone
   （没有隔空投送就发微信/邮件给自己）
2. iPhone 上问存到哪 → 存到「文件」
3. 打开 **AltStore** → 底部 **My Apps** → 左上角 **+**
4. 从「文件」里选中 `AgentBook.ipa`
5. 等它签名安装完成（约 30 秒）
6. 回主屏，看到 **Agent 教材** 图标，点开即可

### 装不上怎么办

| 报错 | 原因与解法 |
| --- | --- |
| `The maximum number of apps for free development profiles has been reached` | 免费账号同时只能装 3 个自签 App，去 AltStore 删掉不用的 |
| `Could not find AltServer` | 电脑上的 AltServer 没运行，或手机和电脑不在同一 Wi-Fi |
| 图标变灰、点不开 | 7 天到期了。让 AltServer 在线，AltStore 会自动续签；也可手动点 Refresh All |
| 提示「无法验证 App」 | 设置 → 通用 → VPN与设备管理 → 信任 |
| 点图标闪退、或提示无法验证 | **开发者模式没开**。见第 3 步开头那段 |

### iOS 26 的额外提醒

系统越新，侧载工具的适配越滞后。如果 AltStore 在你的 iOS 26 上有问题，
换用 **SideStore**（`sidestore.io`）——它用的是同一套免费 Apple ID 签名机制，
但不需要电脑常开（靠手机上的本地 VPN 自己续签），对最新系统的适配通常更快。

两个都试过还不行，就不要再折腾了，直接用 **PWA**（见 `安装说明.md` 的「方案 A」）——
它不挑系统版本，永远不会因为 iOS 升级而失效。

---

## 这件事的日常成本

| 项目 | 频率 |
| --- | --- |
| 首次配置 | 一次，约 30 分钟 |
| 续签 | **自动**（AltServer 在线即可），比手动 ⌘R 省事 |
| 需要你这台 Intel Mac 做什么 | 只是跑着 AltServer，不需要 Xcode、不挑性能 |

---

## 备选：TrollStore（如果你运气好）

如果 iPhone 是 **iOS 14.0–16.6.1 或 17.0**：

1. 不用 AltStore，也不用电脑常开
2. 从 TrollStore 官网按你的系统版本安装 TrollStore
3. 把 `AgentBook.ipa` 传进手机，用 TrollStore 打开
4. **永久有效，不过期，不需要续签**

先去 TrollStore 官网确认支持范围，符合的话这是最省事的方案。
