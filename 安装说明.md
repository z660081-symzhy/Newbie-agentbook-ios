# iPhone / iPad 离线看这本教材

你现在选的是**方案 B：Xcode 原生 App**。下面是从装机到每周续签的完整流程。

---

## 一次性准备

| 需要什么 | 说明 |
| --- | --- |
| 一台 Mac | 装 **Xcode**（App Store 免费，约 10 GB；装完首次启动要它自动装组件，再等十几分钟） |
| 一个 Apple ID | 普通 ID 就行，不用开发者账号 |
| iPhone / iPad | **系统要 iOS 16 或更高**（工程里最低版本设的是 16.0，因为用了原生的分栏导航） |
| 数据线 | 只有**第一次**需要，之后可以走 Wi-Fi |

---

## 第一次安装（约 10 分钟）

1. 打开 `AgentBook/AgentBook.xcodeproj`。
2. 左侧点最上面的蓝色 **AgentBook** → 选 **TARGETS → AgentBook** → **Signing & Capabilities**：
   - 勾上 `Automatically manage signing`
   - **Team**：点下拉 → Add an Account → 用你的 Apple ID 登录（免费）
   - **Bundle Identifier**：改成全局唯一的，比如 `com.你的英文名.agentbook`
     （现在是占位的 `com.example.agentbook`，不改会报签名冲突）
3. iPhone 插上数据线，手机上点「信任此电脑」。
4. Xcode 顶部设备下拉里选中你的 iPhone → 按 **⌘R**。
5. 手机上第一次点图标会打不开，提示「不受信任的开发者」。去
   **设置 → 通用 → VPN与设备管理 → 开发者 App → 信任你的 Apple ID**。
6. 回主屏点图标 —— 装好了，完全离线。

---

## 每周续签：怎么把「插线」省掉

免费 Apple ID 签的 App 有效期是 **7 天**，到期后图标会变灰、点不开。
续签不需要重新走一遍上面的流程，**⌘R 再跑一次即可**。关键在于让它走 Wi-Fi：

### 开启无线调试（做一次就够）

1. iPhone 插着线，连着 Mac。
2. Xcode 菜单 **Window → Devices and Simulators**。
3. 左侧选中你的 iPhone → 勾上 **Connect via network**。
4. 拔掉数据线。左侧设备名旁边会出现一个小地球图标，说明无线连接成功。

> 前提：Mac 和 iPhone 在**同一个 Wi-Fi** 下。
> 只有第一次开这个开关需要插线；如果之后换了 Wi-Fi、或者手机重启后连不上，
> 插一次线重新开一下这个开关就行。

### 每周的动作

> iPhone 上图标变灰 → 拿起 Mac，打开项目 → ⌘R → 手机上图标恢复，继续看。

整个过程不用插线，通常 30 秒内完成（首次编译会慢一些，之后走增量编译很快）。

### 两个常见的坑

| 现象 | 原因与解法 |
| --- | --- |
| ⌘R 报错「Unable to install... provisioning profile」 | 7 天证书过期了，Xcode 会自动重新签；如果没自动，去 Signing 里把 Team 取消再重新选一次 |
| 设备下拉里找不到 iPhone | 检查是否同一 Wi-Fi；或插一次线重新开 Connect via network |

### 数据会丢吗？

**通常不会。** 续签是"用同一个 Bundle ID 重新装一次"，iOS 把它当成 App 升级，数据容器保留。
但这毕竟不是 100% 保证的机制，所以我在 App 里做了**备份与恢复**：
右上角的分享图标 → 复制备份码（一段文本，存备忘录里）→ 万一数据没了，粘回去就能恢复书签和阅读位置。

---

## 这个 App 里有什么

### 原生部分（SwiftUI）

| 组件 | 做什么 |
| --- | --- |
| `NavigationSplitView` | iPad / Mac 横屏是左右分栏；iPhone 自动折叠成「目录 → 正文」 |
| `List` + `.sidebar` 样式 | 按「第 0 部分…第 5 部分」分组的原生目录（46 条，含 Todolist 阶段和附录） |
| `.searchable` | 原生搜索框，搜章节标题或所属部分 |
| `ProgressView` | 顶部学习进度条「已读 12 / 46」，点一下能继续上次读到的地方 |
| `Menu` | 下拉切主题：跟随系统 / 浅色 / 深色 |
| 工具栏 | 加书签、备份与恢复 |
| `Form` + `TextField` | 备份码的导出与导入 |

### 内容部分（WKWebView）

整本教材就是一个 900 KB 的 `book.html` 打包在 App 里，**完全离线**：
34 章正文、47 个可拖动旋转的三维演示、34 个交互动画、322 道自测题（带答案和错题本）、
263 张术语闪卡、18 处面试话术演练。

### 原生和网页怎么打通的

- 你在网页里往下滚 → 网页用 `IntersectionObserver` 捕捉当前章节 → 通过 `WKScriptMessageHandler`
  报给原生 → 侧边栏勾选和进度条实时跟着动
- 你在侧边栏点一章 → 原生发一条 JS 消息 → 网页滚过去并闪一下高亮
- 你切主题 → 原生改自己的配色，同时把 `data-theme` 注入网页，两边永远一致
- 侧边栏的「已读」**直接镜像网页自己的"标记已读"状态**，不是另存一份

### 网页端顺手做的加固

原来代码直接调 `localStorage`，而 WKWebView 的 file:// 页面在个别情况下这个调用会抛异常，
一旦抛出整个脚本就崩、页面白屏。现在改成 `try/catch` + 内存兜底：存不进去也不会崩。

---

## 我验证了什么，没验证什么

**这台机器只装了 Command Line Tools，没有 Xcode，也没有 iOS SDK，所以我无法编译这个工程。**
为了尽量把风险降下来，我做了这些检查：

| 检查项 | 结果 |
| --- | --- |
| 五个 Swift 文件语法解析（`swiftc -parse`） | 通过 |
| 模型层类型检查（`Chapter.swift` + `BookStore.swift`，macOS SDK 真实类型检查） | 通过，零错误零警告 |
| View 层类型检查（`ContentView.swift`，用替身绕开 iOS 专有 API） | 通过，零错误 |
| 工程文件语法（`plutil -lint project.pbxproj`） | 通过 |
| 工程引用的 8 个文件是否都存在、路径是否正确 | 全部存在 |
| 注入网页的 57 行桥接脚本 JS 语法（`node --check`） | 通过 |
| `toc.json` 结构（46 条、id c1–c46 连续） | 正确 |

**唯一没被类型检查覆盖的是 `BookWebView.swift`**（`UIViewRepresentable` + WebKit 是 iOS 专有，
macOS 上没法编译）。这个文件我逐行复核过两遍，并在写的过程中修掉了两个真实 bug：
主题在页面加载完成前就被"标记为已应用"导致永远不生效、以及首次启动时"跳到上次位置"会被提前消费掉。

如果 Xcode 还是报错，**把报错原文发我**，我照着改 —— 有报错就能定位，比猜快得多。

---

## 附：另外两条路（备用）

### 方案 A：PWA（不用 Xcode，也不会过期）

把 `web/` 里 6 个文件传到任意静态托管（GitHub Pages 免费），iPhone 用 Safari 打开、等 3 秒、
点「添加到主屏幕」。之后从主屏点开是全屏无地址栏，**断网也能用**，而且永不过期。
代价是：进度数据依赖"别清理 Safari 数据"。

> 这条路我实测过两次：起本地服务 → 装 Service Worker → 模拟断网重载 → 页面完整渲染（47 个三维、322 道题）。

### 方案 C：a-Shell 临时体验

iOS 上装免费 App「a-Shell」，把 `web` 放 iCloud Drive，跑：

    cd ~/Documents/web
    python3 -m http.server 8000

Safari 打开 `http://localhost:8000`。缺点：每次冷启动都要重跑一遍。

---

## 文件清单

```
ios/
├── 安装说明.md                 本文件
├── AgentBook/                  方案 B：双击 AgentBook.xcodeproj
│   ├── AgentBook.xcodeproj/project.pbxproj
│   └── AgentBook/
│       ├── AgentBookApp.swift          入口
│       ├── ContentView.swift           原生外壳：目录、搜索、进度、书签、主题、备份
│       ├── BookWebView.swift           WKWebView 封装 + 原生↔网页桥
│       ├── BookStore.swift             学习状态（进度镜像、书签、主题、备份码）
│       ├── Chapter.swift               目录模型
│       ├── toc.json                    46 条目录（构建时生成）
│       ├── book.html                   教材本体（构建时生成）
│       └── Assets.xcassets/AppIcon.appiconset/icon-1024.png
└── web/                        方案 A：把这 6 个文件传到静态托管
    ├── index.html
    ├── sw.js                   Service Worker：缓存优先 + 后台自动更新
    ├── manifest.webmanifest
    ├── icon-180.png
    ├── icon-192.png
    └── icon-512.png
```
